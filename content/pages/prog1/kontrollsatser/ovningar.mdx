---
title: "Kontrollsatser"
date: "2021-08-30"
---

Gör övningarna i Visual Studio Code. Skapa en ny fil, namnge den *controlExcercises.js* och gör övningarna i den filen.

Övningar behöver inte lämnas in.

# Grunden
Klistra in följande kod i din fil och använd den för att lösa övningarna.

```javascript
let myName = "Alfred";
let age = 18;
let drivingAge = 18;
let pensionAge = 65;

// Följande kod är ett snabbare sätt att sätta flera variabler
// Man skriver bara en "let", gör en deklaration, och sätter en kommatecken för att kunna göra en till deklaration.
let saldo = 12,
  pay = 20,
  subscriptionCost = 6,
  amountOfSubs = 3,
  snacksCost = 2;

let totalSubCost = subscriptionCost * amountOfSubs;

console.log(Alfred + " är " + age + "år.");
console.log("Man får köra bil när man är " + drivingAge);
console.log("När man är " + pensionAge + "år kan man gå i pension.");

```


# Övning 1
Skapa en kontrollsats (`if()`), kolla om variabeln `age` är samma som variabeln `drivingAge`.
Skriv ut ett meddelande i konsolen om `age` är lika med `drivingAge` som säger att Alfred är gammal nog att kunna skaffa körkort.

<PracticeContainer>

```javascript
if(age == drivingAge) {
  console.log(Alfred + " är gammal nog att skaffa körkort.");
}
```

</PracticeContainer>


# Övning 2
Skapa en till kontrollsats som kollar om `age` är lägre än `pensionAge`.
Skriv ut ett meddelande i konsolen om `age` är lägre, som säger att Alfred inte är i pensionsåldern än.
Bonus: Skriv ut hur många år Alfred har kvar till pensionsåldern.

<PracticeContainer>

```javascript
if(age < pensionAge) {
  console.log("Alfred är inte i pensionsåldern än.");
  console.log("Alfred har " + (pensionAge-age) + " år kvar tills pensionen."); //Bonus
}
```

</PracticeContainer>

# övning 3
1. Sätt variabeln saldo till sig själv plus pay.
2. Skapa sedan en variabel som heter monthlyExpenses, och sätt dess värde till `totalSubCost+snacksCost*3`.
3. Använd en kontrollsats för att kolla om Alfreds månadskostnader (monthlyExpenses) överskrider eller blir lika med hans saldo, om den gör det ska skriptet skriva ut ett meddelande om konsolen som varnar honom om detta.
4. Ändra variablerna tills dess att ditt meddelande dyker upp i konsolen.

<PracticeContainer>

```javascript
saldo += pay; // Det samma som saldo = saldo + pay;
let montlyExpenses = totalSubCost + snacksCost * 3;

if(monthlyExpenses >= saldo) {
  console.log("Varning! Dina månadskostnader överskrider eller är lika med ditt saldo");
}
```

</PracticeContainer>

# Övning 4


# Övning 5

# Övning 6

# Övning 7

# Övning 8

# Övning 9

# Övning 10

Kolla om två tal är lika
Kolla om ett tal är högre än ett annat tal
Kolla om ett tal är lägre än ett annat tal

Kolla om ett tal är högre än ett specifikt tal, men lägre än ett annat specifikt tal (&&)

Kolla om ett tal är en av två tal (||)

Använda modulus för att kolla om ett tal är delbart med ett annat tal
Använda modulus för att kolla om ett tal är jämt eller inte

https://www.w3resource.com/c-programming-exercises/conditional-statement/index.php

https://www.w3resource.com/javascript/javascript.php

## Får Kalle Kanin gå på sommarläger?
1. Det har nu gått några år och Kalle är äldre. Börja med att lägg till 5 till Kalles ålder.
2. Skapa en variabel för åldern man behöver vara för att få åka på sommarläger. Sätt variabeln till 10.
3. Skapa en kontrollsats som kollar om Kalles ålder är högre än åldern man behöver vara för att få åka på sommarläger.

<PracticeContainer>

```javascript
age += 5;
let summerCampAge = 10;

if(age >= summerCampAge) {
  console.log("Jag får gå på sommarläger!");
}
```

</PracticeContainer>

---

## Vad händer om Kalle inte får åka på sommarläger?
Lägg till en else-sats till din if-sats, som skriver ut något liknande "Jag får inte åka på sommarläger". Som en bonus kan du skriva ut hur många år det är kvar tills Kalle får åka på sommarläger.

<PracticeContainer>

```javascript
if(age >= summerCampAge) {
  console.log("Jag får gå på sommarläger!");
} else {
  console.log("Det är " + summerCampAge - age " tills jag får åka på sommarläger.");
}
```

</PracticeContainer>

---

## Får Kalle Kanin åka Berg- och Dalbana?
För att få åka berg- och dalbana så behöver man vara minst 11 år, och över 90cm lång.

Skapa en kontrollsats som kollar både Kalles ålder och höjd, och skriver ut om han får åka berg- och dalbana.

Tips: För detta behöver du operanden `&&`

<PracticeContainer>

```javascript
if(age >= 11 && height > 90) {
  console.log("Kalle får åka berg- och dalbana!")
}
```

</PracticeContainer>

---

# Får Kalle åka stora vattenrutchkanan?

För att få åka vattenrutchkana så behöver man antingen vara minst 10 år gammal, eller vara 120cm eller över.

Skapa en kontrollsats som kollar både Kalles ålder och höjd, och skriver ut om han får åka berg- och dalbana.

Tips: För detta behöver du operanden `||`

<PracticeContainer>

```javascript
if(age >= 10 || height >= 120) {
  console.log("Kalle får åka vattenrutchkanan!");
}
```

</PracticeContainer>