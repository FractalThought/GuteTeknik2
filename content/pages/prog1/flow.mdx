---
title: "Folders, Flödet, och variabler"
date: "2020-08-20"
---

Beskrivning: Vi går igenom vikten av att hantera sina folders och filer, och börjar sedan skriva och köra kod som är sparad på en fil. Vi går även igenom datatyper (number, string, boolean).

Moment: code-along, genomgång, övningar

# Grundläggande folder och projekthantering

- Skapa folders där vi kommer ha våra projekt.
- Hur vi öppnar en folder i VSCode.
- Hur vi skapar ett nytt projekt.
- Skapa folders och filer i VSCode.

# Öva på flödet

Vi startar ett projekt genom att öppna upp Visual Studio Code och navigera till vår projektfolder, och skapar en ny folder (intro) som vi öppnar.

I denna foldern skapar vi en ny fil som vi döper till main.js, och öppnar den filen.

Här kan vi nu skriva kod, och köra den. Vi börjar med att testa Hello World:

## Hello World

Vi skriver `console.log("Hello World");` i filen och sparar med kortkommandot Ctrl+S.

Sedan visar vi upp terminalen/konsolen (View > Terminal).

Slutligen går vi till terminalen och skriver in `node main.js` (filens namn)

## Flera rader

Vi kan kopiera denna raden kod genom att markera hela raden (med musen, eller med kortkommandot Ctrl+Shift+Pil höger/vänster några gånger [Kortkommandot markerar hela ord istället för karaktär]). Använd kortkommandot Ctrl+C för att kopiera det du markerat.

Skapa sedan en ny rad (Entertangenten) och klistra in din kopierade rad med kortkommandot Ctrl+V.

Vi kan också duplicera raden med kortkommandot Alt+Shift+Pil upp eller Pil ner.

Ändra nu så att andra raden säger något annat, till exempel ditt namn.

Det gör du genom att ta bort Hello World inuti citattecknen ("") och skriva något annat där istället.

Gå sedan tillbaka till terminalen och skriv in `node main.js` (filens namn).

Notera vad som händer - ett flöde sker där datorn kör en rad efter den andra.

Upprepa och testa på att använda kortkommandot eller kopiera och klistra in några gånger. Bli bekväm med detta då du kommer använda det väldigt mycket.

## Byt ordning på raderna

Nu när vi har fler rader så kan vi testa vad som händer när vi byter ordningen på raderna.

Du kan göra på liknande sätt som du gjorde tidigare - markera hela raden och använd kortkommandot Ctrl+X istället för C för att klistra ut. Gå sedan upp med piltangenterna och klistra in med kortkommandot Ctrl+V igen.

Om du vant dig med kortkommandot Ctrl+Shift+pil upp/ner för att duplicera en rad, så kan du testa att skippa Shift (alltså bara Ctrl+pil upp/ner) för att flytta en rad. Det är ett väldigt effektivt och snabbt sätt att arbeta på.

# Variabler

## Vad är en variabel?

Hittills har vi bara låtit datorn ta in ett input, utvärdera den, och skriva ut resultatet. Men vi har inte lagrat något.

För att lagra något så behöver vi skapa en variabel. En variabel består av två saker; Dess namn, och dess värde. Ni kommer kanske ihåg variabler från matematiken, där man oftast säger till exempel att x är lika med ett värde, till exempel 5. Variabler i programmering fungerar på samma sätt.

För att skapa en variabel i javascript så behöver vi använda ett nyckelord som heter "let". Med det ordet så säger vi till datorn att den ska låta det vi namnger variablen till (namnet), att vara det vi sätter variabeln till (värdet).

```jsx
let name = "Felix";
let age = 29;
```

Detta skapar variabeln *name*, som har värdet *"Felix"*, och variabeln *age* med värdet *29*.

När vi använder variabeln så behöver vi då endast använda dess namn, så här:

```jsx
console.log(name);
console.log(age);
```

Ovanstående kod skriver ut värdena som vi satte tidigare.

## Sätta om variabler

En variabel kan "sättas om", alltså att vi kan låta en variabel hålla ett annat värde än det ursprungliga värdet vi satte. Då behöver vi bara använda variabelns namn, utan let-nyckelordet;

```jsx
name = "Erik";
age = 30;
```

## Operand till variabler

Vi kan lägga ihop två variabler, eller en variabel och ett värde. Till exempel:

```jsx
let numberOne = 5;
let numberTwo = 10;
let sum = numberOne + numberTwo;
```

Detta fungerar främst för nummer, men som vi tidigare upptäckt så kan vi lägga ihop meningar. Detta kallas konkatenering;

```jsx
let firstName = "Felix";
let lastName = "Thålin";
let fullName = firstName + " " + lastName;
```

Med nummer så kan vi utföra många fler uträkningar, och med variabler så finns det några ytterligare operander som vi kan använda oss av.

Till exempel så har vi `++` som används för incrementering, alltså att lägga till 1 till ett existerande värde. Till exempel så här:

```jsx
let age = 17;
age++;
console.log(age);
```

Resultatet av ovanstående kod blir att terminalen skriver ut 18.

Vi kan även lägga till specifika värden till en variabel med `+=` operanden (kallas additionstilldelning). Till exempel så här:

```jsx
let money = 10;
money += 5;
console.log(money);

let pay = 10;
money += pay;
console.log(money);
```

Resultatet av ovanstående kod blir att terminalen skriver ut 15, och sedan 25.

# Övningar på variabler i VS Code

## Information om dig

1. Skapa variabler för följande värden: Ditt namn, din ålder, din ålder om 5 år.
2. Skriv ut variablerna på följande sätt: "Hej! Jag heter ..., och jag är ... år och om 5 år är jag ..." där varje ... är en variabel.
3. Använd nu din ålder för att ta reda på ditt födelseår och skriv ut den.

## Kalle Kanin och Igge Igelkott

1. Skriv ett script som ska skriva ut följande, i korrekt ordning, och använder variabler för namn och ålder:

```markdown
Hej! Jag heter Kalle Kanin och jag är 6 år gammal.
Min bror heter Kasper Kanin och är dubbelt så gammal som jag, han är 12 år gammal.
Min lillasyster heter Klara Kanin och är bara 3 år gammal.
Mamma och pappa Kanin säger att när jag är 10 år så får jag åka på sommarläger.
Det är 4 år tills dess.
```

2. Ändra sedan så att du inte skriver ut syskonens ålder i kod (utskriften kommer som ett resultat av att du lagt ihop variabler).

3. Ändra nu så att det istället skrivs ut följande:

```markdown
Hej! Jag heter Kalle Kanin och jag är 6 år gammal.
Mamma och pappa Kanin säger att när jag är 10 år så får jag åka på sommarläger.
Det är 4 år tills dess.
Min lillasyster heter Klara Kanin och är bara 3 år gammal.
Min bror heter Kasper Kanin och är dubbelt så gammal som jag, han är 12 år gammal.
```

4. Lägg till en ny sektion där följande står:

```markdown
Hej! Jag heter Igge Igelkott och jag är 8 år gammal.
Mamma och pappa Igelkott säger att när jag är 10 år så får jag åka på sommarläger.
Det är 2 år tills dess.
Min lillasyster heter Isabella Igelkott och är bara 4 år gammal.
Min bror heter Isak Igelkott och är dubbelt så gammal som jag, han är 16 år gammal.
```

5. Säg att du skulle lägga till ytterligare en sektion till ett till som har en likadan familj. Hur mycket skulle du behöva ändra, och hur mycket tid kan du spara med variabler?

Testa gör en ny sektion för **Saga Svala** som är **4** år gammal och har en storebror som heter **Sture**, och en lillasyster som heter **Sara**.

<PracticeContainer>
Om vi sätter följande variabler:

- *firstName*
- *lastName*
- *littleSister*
- *bigBrother*
- *age*

Så kan vi använda dessa för att få ut all information som vi behöver skriva ut.

För att skapa en ny sektion så behöver vi då bara ändra variablerna till det nya värdena, och kopiera console.log-raderna vi tidigare skrivit efter variablerna blivit ändrade.
</PracticeContainer>
