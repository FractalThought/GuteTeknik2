---
title: "Kontrollsatser"
date: "2020-09-01"
---

Kontrollsatser använder vi för att kontrollera flödet utifrån oftast externa variabler. Vad det betyder är att vi kan låta vissa delar av koden köras bara under särskilda förutsättningar.

Den mest grundläggande kontrollsatsen är en ifsats;

```javascript
if (true) {
  console.log("Detta körs");
}

if (false) {
  console.log("Detta körs inte");
}
```

Som du ser så är satsen uppbyggd i delar;

`if` är ett nyckelord som används för att visa att detta är en ifsats.

`(true)` är ett så kallat *statement*, som antingen är sant eller falskt. Om det är sant, så körs koden som finns i ifsatsen, annars körs den inte.

`{ }` används för att kapsla in den kod som ska köras om ifsatsens statement är sant.

# Övningar

## 1. Grunden
Fortsätt med practice.js-filen vi skapat tidigare. Lägg till variabler för Kalles höjd. Kalle är 100cm lång, det räcker att du skriver `100`.

Om du saknar filen, gå till [Flow-övningen](https://guteteknik.netlify.app/prog1/flow), längst ner i lösningen så finns all kod. Kopiera in den i en fil i Visual Studio Code.

<PracticeContainer>
```javascript
let height = 100;
```
</PracticeContainer>

## Får Kalle Kanin gå på sommarläger?
1. Det har nu gått några år och Kalle är äldre. Börja med att lägg till 5 till Kalles ålder.
2. Skapa en kontrollsats som kollar om Kalles ålder är högre än åldern man behöver vara för att 

<PracticeContainer>
```javascript
age += 5;

if(age >= summerCampAge) {
  console.log("Jag får gå på sommarläger!");
}
```javascript
```

</PracticeContainer>

## Får Kalle Kanin åka Berg- och Dalbana?
För att få åka berg- och dalbana så behöver man vara minst 11 år, och över 90cm lång.

Skapa en kontrollsats som kollar både Kalles ålder och höjd, och skriver ut om han får åka berg- och dalbana.

<PracticeContainer>

```javascript
if(age >= 11 && height > 90) {
  console.log("Kalle får åka berg- och dalbana!")
}
```
</PracticeContainer>

# Får Kalle åka stora vattenrutchkanan?

För att få åka vattenrutchkana så behöver man antingen vara minst 10 år gammal, eller vara 120cm eller över.

Skapa en kontrollsats som kollar både Kalles ålder och höjd, och skriver ut om han får åka berg- och dalbana.

<PracticeContainer>
```javascript
if(age >= 10 || height >= 120) {
  console.log("Kalle får åka vattenrutchkanan!");
}
```
</PracticeContainer>